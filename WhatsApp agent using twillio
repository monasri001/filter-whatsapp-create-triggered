WhatsApp AI Agent (Beginner Friendly)

🚀 What this project does:

Connects WhatsApp using Twilio

Receives messages

Sends AI-generated replies (using OpenAI API)

Runs on your local machine



---

🗂️ Project Structure

whatsapp-agent/
│
├── app.py
├── requirements.txt
├── .env
└── README.md


---

🧾 1. requirements.txt

flask
twilio
python-dotenv
openai


---

🔐 2. .env (IMPORTANT)

Create a .env file:

TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
OPENAI_API_KEY=your_openai_api_key


---

🧠 3. app.py (Main Code)

from flask import Flask, request
from twilio.twiml.messaging_response import MessagingResponse
from dotenv import load_dotenv
import os
from openai import OpenAI

# Load environment variables
load_dotenv()

# Initialize app
app = Flask(__name__)

# OpenAI client
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# Route for WhatsApp messages
@app.route("/whatsapp", methods=["POST"])
def whatsapp_reply():
    incoming_msg = request.values.get('Body', '').lower()

    # AI Response
    response = client.chat.completions.create(
        model="gpt-4.1-mini",
        messages=[
            {"role": "system", "content": "You are a helpful WhatsApp assistant."},
            {"role": "user", "content": incoming_msg}
        ]
    )

    reply = response.choices[0].message.content

    # Twilio response
    twilio_resp = MessagingResponse()
    twilio_resp.message(reply)

    return str(twilio_resp)


if __name__ == "__main__":
    app.run(port=5000, debug=True)


---

📘 4. README.md

# WhatsApp AI Agent

## Setup

1. Install dependencies
   pip install -r requirements.txt

2. Add your credentials in .env

3. Run server
   python app.py

4. Use ngrok to expose local server
   ngrok http 5000

5. Paste ngrok URL in Twilio sandbox webhook:
   https://your-ngrok-url/whatsapp

## Done 🎉


---

⚙️ Setup Instructions (Step-by-Step)

1. Install dependencies

pip install -r requirements.txt


---

2. Run the server

python app.py


---

3. Start ngrok (for public URL)

ngrok http 5000


---

4. Setup Twilio WhatsApp Sandbox

Go to Twilio WhatsApp Sandbox

Paste:


https://your-ngrok-url/whatsapp

into WHEN A MESSAGE COMES IN


---

5. Send message from WhatsApp ✅

You’ll get AI replies instantly.


---

💡 What You Learn From This Project

Webhooks

API integration

WhatsApp automation

AI agent basics

Flask backend



---

🔥 Want Next Level?

After this, you can upgrade to:

Memory-based chatbot

Customer support bot

SOS alert system (your project idea 👀)

RAG-based WhatsApp bot


